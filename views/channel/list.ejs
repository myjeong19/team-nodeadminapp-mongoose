<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>INSPINIA | Main view</title>

  <link href="/css/bootstrap.min.css" rel="stylesheet" />
  <link href="/font-awesome/css/font-awesome.css" rel="stylesheet" />

  <link href="/css/animate.css" rel="stylesheet" />
  <link href="/css/style.css" rel="stylesheet" />

  <!--추가 스타일-->
</head>

<!--우측 상단 헤더 영역 -->
<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-10">
    <h2>사용자 정보 관리</h2>
    <ol class="breadcrumb">
      <li>
        <a href="/Home/Index">홈</a>
      </li>
      <li>
        <a>사용자 정보 관리</a>
      </li>
      <li class="active">
        <strong>조회</strong>
      </li>
    </ol>
  </div>
  <div class="col-lg-2"></div>
</div>

<!--우측 콘텐츠 영역 -->
<div class="wrapper wrapper-content">
  <div class="ibox-content m-b-sm border-bottom">
    <div class="row">
      <div class="col-sm-4">
        <div class="form-group">
          <label class="control-label" for="OrderNo">사용자 명</label>
          <input type="text" class="form-control" />
        </div>
      </div>
      <div class="col-sm-4">
        <div class="form-group">
          <label class="control-label" for="OrderName">아이디</label>
          <input type="text" class="form-control" />
        </div>
      </div>
      <div class="col-sm-4">
        <div class="form-group">
          <label class="control-label" for="MobileNo">사용여부</label>
          <input type="text" class="form-control" />
        </div>
      </div>
    </div>
    <div class="text-center">
      <button type="button" class="btn btn-primary">조회</button>
      <a href="/channel/create" class="btn btn-info">신규</a>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-content">
          <div class="table-responsive">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th width="10%">커뮤니티 고유번호</th>
                  <th width="5%">채널 분류 코드</th>
                  <th width="10%">채널명</th>
                  <th width="10%">채널 간략 소개</th>
                  <th width="10%">동시 채널 접속자 수</th>
                  <th width="5%">대표 이미지</th>
                  <th width="10%">채널 오픈 상태 코드</th>
                  <th width="10%">등록일시</th>
                  <th width="10%">등록자 아이디</th>
                  <th width="10%">수정일시</th>
                  <th width="10%">수정자 아이디</th>
                </tr>
              </thead>
              <tbody class="hoverTblBody">
                <% for (let i = 0; i < channels.length; i ++) { %>

                <tr>
                  <td>
                    <a href="/channel/modify/<%= channels[i].channel_id %>"
                      ><%= channels[i].channel_id %></a
                    >
                  </td>
                  <td><%= channels[i].community_id %></td>
                  <td><%= channels[i].channel_name %></td>
                  <td><%= channels[i].channel_desc %></td>
                  <td><%= channels[i].user_limit %></td>
                  <td>
                    <img
                      class="w-50"
                      src="<%= channels[i].channel_img_path %>"
                      alt="<%= channels[i].channel_name %> 채널 대표 이미지"
                    />
                  </td>
                  <td><%= channels[i].channel_state_code %></td>
                  <td><%= channels[i].reg_date %></td>
                  <td><%= channels[i].reg_member_id %></td>
                  <td><%= channels[i].edit_date %></td>
                  <td><%= channels[i].edit_member_id %></td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
